import WeatherController from './controller.js';

document.addEventListener('DOMContentLoaded', () => {
    // Initialize the WeatherController once the DOM is loaded
    WeatherController.init();
});
